import { StyleSheet } from "react-native";

export const actionButtonStyles = StyleSheet.create({
  actionButton: {
    flex: 1,
    alignItems: "center",
    paddingVertical: 8,
  },
  actionText: {
    fontSize: 14,
    color: "#65676b",
    fontWeight: "500",
  },
});
